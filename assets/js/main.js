const { createApp } = Vue;
createApp({
    data() {
      return {
        /* variables Vue 3 */
        message: 'Hello Vue!'
      }
    },
    methods: {
        /* functions Vue 3 */
        
    }
  }).mount('#app')
